<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습 index</title>
  </head>
  <body>
    <h2>POST 요청 FORM</h2>
    <form action="/practice" method="post">
      <label for="name">이름</label>
      <input type="text" id="name" name="name" required />

      <fieldset>
        <legend>성별</legend>
        <!-- required 속성을 두 radio 버튼 중 하나에만 적용해도 됨 -->
        <label><input type="radio" name="gender" value="남자" required />남자</label>
        <label><input type="radio" name="gender" value="여자" />여자</label>
      </fieldset>
      <br />
      <fieldset>
        <legend>생년월일</legend>
        <select name="yy" id="year">
          <%for(let i = 1985; i <= 2024; i++){%>
          <option value="<%=i%>"><%=i%></option>
          <%}%>
        </select>
        <span>년</span>
        <select name="mm" id="month">
          <%for(let i = 1; i <= 12; i++) {%>
          <option value="<%=i%>"><%=i%></option>
          <%}%>
        </select>
        <span>월</span>
        <select name="dd" id="day">
          <%for(let i = 1; i <= 31; i++) {%>
          <option value="<%=i%>"><%=i%></option>
          <%}%>
        </select>
        <span>일</span>
      </fieldset>

      <br />
      <fieldset>
        <legend>관심사</legend>
        <label><input type="checkbox" name="interest" value="여행" />여행</label>
        <label><input type="checkbox" name="interest" value="패션" />패션</label>
        <label><input type="checkbox" name="interest" value="음식" />음식</label>
      </fieldset>
      <br />

      <fieldset>
        <legend>선택</legend>
        <label>좋아하는 색상 <input type="color" name="likeColor" value="좋아하는 색" /></label>
        <br />
        <label>좋아하는 숫자 <input type="number" name="likeNumber" value="좋아하는 숫자" /></label>
      </fieldset>
      <br />
      <input type="submit" value="회원가입" />
    </form>
  </body>
</html>
