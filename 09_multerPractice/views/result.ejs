<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>파일 업로드 결과 페이지</title>
    <style>
      .container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        text-align: center;
        margin: 20px;
        padding: 50px;
        width: 30%;
        border-radius: 10px;
        box-shadow: 0 0 8px lightgray;
      }

      h2 > span {
        color: rgb(0, 119, 255);
      }

      img {
        box-shadow: 0 0 5px pink;
        margin-bottom: 20px;
      }

      .result > div {
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        text-align: center;
      }

      .result > div > span {
        display: inline-block;
        width: 100px; /* 아이디, 비밀번호, 이름, 나이 등의 너비를 조정하고 싶다면 원하는 값으로 설정하세요 */
      }

      .result > div > span + span {
        margin-left: -35%; /* 아이디와 비밀번호 사이의 간격을 조정하기 위해 마진을 추가 */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2><span><%= name%></span>님 마이페이지</h2>
      <div>
        <img class="profile" src="<%= imgSrc%>" alt="기본 프로필 이미지" width="200" height="200" />
      </div>
      <div class="result">
        <div><span>아이디</span><span><%= id%></span></div>
        <div><span>비밀번호</span><span><%= pw%></span></div>
        <div><span>이름</span><span><%= name%></span></div>
        <div><span>나이</span><span><%= age%></span></div>
      </div>
    </div>
  </body>
</html>
