<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- axios CDN -->
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <!-- custom JS -->
    <script src="/static/visitor.js" defer></script>
    <title>방명록 페이지</title>
  </head>
  <body>
    <h1>방명록 페이지</h1>
    <form name="visitor-form">
      <fieldset>
        <legend>방명록 등록</legend>
        <input type="text" name="name" placeholder="사용자 이름" />
        <br />
        <input type="text" name="comment" placeholder="간단 방명록 작성" />
        <br />
        <div id="btn-group">
          <button type="button" onclick="createVisitor()">방명록 등록</button>
        </div>
      </fieldset>
    </form>
    <br /><br />
    <table border="1" cellspacing="0">
      <thead>
        <tr>
          <th>ID</th>
          <th>작성자</th>
          <th>방명록</th>
          <th>수정</th>
          <th>삭제</th>
        </tr>
      </thead>
      <tbody>
        <!-- <tr>
          <td>1</td>
          <td>윤영인</td>
          <td>안녕하세요~~</td>
          <td>
            <button type="button">수정</button>
          </td>
          <td>
            <button type="button">삭제</button>
          </td>
        </tr> -->

        <!-- for문 작성 -->
        <!-- (controller에서 data로 데이터 객체 넘겨줌)
          data: [{id, name, comment}, {}, ..., {}] -->
        <%for(let i = 0; i < data.length; i++) {%>
        <tr id="tr_<%= data[i].id%>">
          <td><%= data[i].id%></td>
          <td><%= data[i].name%></td>
          <td><%= data[i].comment%></td>
          <td>
            <button type="button" onclick="editVisitor('<%=data[i].id%>')">수정</button>
          </td>
          <td>
            <button type="button" onclick="deleteVisitor(this,'<%=data[i].id%>')">삭제</button>
          </td>
        </tr>
        <%}%>
      </tbody>
    </table>
  </body>
</html>
